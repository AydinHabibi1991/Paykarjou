/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['api.peykarjou.com', 'peykarjou.com', 'picsum.photos'],
  },
};

module.exports = nextConfig
